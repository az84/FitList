show username
show list of built workouts

</br></br></br>
  </thead>
     {{#each workouts as |workout| }}
 {{!--    {{log 'workout' workout}} --}}
{{log 'workout' workout}}
{{log 'workout.exercises' workout.exercises}}

<div class="row">
  <div class="col-sm-6">
    <div class="card">
      <h5 class="card-header">Workout Name: {{workout_name}}</h5>
      <div class="card-body">
         <ul class="list-group list-group-flush">
    <li class="list-group-item">Exercise count {{this.exercises.length}}</li>
    <li class="list-group-item">A second item</li>
    <li class="list-group-item">A third item</li>
   </ul>
      </div>
    </div>
  </div>
  {{log 'workout.exercises.name' workout.exercises.name}}
    {{#if workout.exercises.length}}  
      {{#each workout.exercises as |exercise|}}  
      {{log 'exercise' exercise}}
    <div class="col-sm-6">
    <div class="card">
      <h5 class="card-header">{{this.name}}</h5>
      <div class="card-body">
        <ul class="list-group list-group-flush"></ul>
         {{#if exercise.equipment}} <li class="list-group-item">equipment: {{exercise.equipment}}</li>{{/if}}
         {{#if type}} <li class="list-group-item">type: {{type}}</li>{{/if}}
         {{#if muscle}} <li class="list-group-item">muscle: {{muscle}}</li>{{/if}}
         {{#if sets}} <li class="list-group-item">sets: {{sets}}</li>{{/if}}
         {{#if reps}} <li class="list-group-item">reps: {{reps}}</li>{{/if}}
         {{#if weight}} <li class="list-group-item">weight: {{weight}}</li>{{/if}}
         {{#if distance}} <li class="list-group-item">distance: {{distance}}</li>{{/if}}
         {{#if duration}} <li class="list-group-item">duration: {{duration}}</li>{{/if}}
        </ul>
      </div>
    </div>
  </div>
  
  {{/each}}
     {{/if}}
   
</div>

  {{/each}}